# ملاحظة: بحث اللاعبين بالاسم في شاشة الذهب المركزي — مكتمل

هذا استكمال للبند الثالث المتبقي من طلب العميل: "استكمال شاشة أدمن الذهب
المركزي (التحويل بين لاعبين بالبحث)". الحالة: **مكتمل من ناحية الكود**،
لسه محتاج اختبار فعلي على سيرفر PHP حقيقي (نفس وضع نظام الـ Feeding).

## المشكلة اللي كانت موجودة
شاشة `centralGold.tpl` (لوكاب + تحويل) كانت تطلب من الأدمن **كتابة الإيميل
يدويًا** بالكامل. ده غير عملي لأن حساب الذهب المركزي منفصل عن قاعدة بيانات
أي عالم (`world`) بعينها — الأدمن غالبًا هيعرف اسم اللاعب فقط، مش الإيميل
اللي سجّل بيه (وممكن يكون سجّل من عالم تاني أصلاً).

## الحل
1. **`GameEngine/CentralGold.php`** — دالة جديدة `searchByUsername($query,
   $limit = 10)`: بحث `LIKE` جزئي (case-insensitive افتراضيًا حسب الـ
   collation) على `central_gold_accounts.username`، مع escaping صريح
   لرموز `LIKE` الخاصة (`%`, `_`, `\`) اللي ممكن اللاعب يكتبها بالغلط، و
   prepared statement زي باقي الكلاس بالضبط.
2. **`GameEngine/Admin/Mods/centralGoldAdmin.php`** — أكشن جديد
   `?do=search&q=...` (GET، بيرجع JSON). اتحط **قبل** استدعاء
   `csrf_verify()` لأن الأخيرة بتفحص `$_POST` بس وهترفض أي GET تلقائيًا؛
   الحماية بدل الـ CSRF token هنا هي إعادة فحص صلاحية الأدمن (access ≥ 9)
   من قاعدة البيانات، اللي بيحصل فعليًا قبل نقطة الـ search مباشرة — يعني
   العملية للقراءة فقط (read-only) ومحمية بنفس قوة باقي الصفحة.
3. **`Admin/Templates/centralGold.tpl`** — حقل بحث (autocomplete) فوق كل
   حقل إيميل (في اللوكاب، وفي From/To بتاع التحويل). عند الكتابة (بحد أدنى
   حرفين، بعد debounce 250ms) بيستدعي `?do=search`، ويعرض قائمة نتائج
   (اسم + إيميل + رصيد الذهب المدفوع)، وبالضغط على نتيجة بيتملى حقل
   الإيميل الحقيقي تلقائيًا. حقل الإيميل الأصلي **لسه موجود ومطلوب** كـ
   fallback يدوي لو الأدمن عارف الإيميل مباشرة أو نتيجة البحث مش دقيقة.
4. **`GameEngine/Lang/en.php`** — 4 ثوابت لغة جديدة بنفس نمط باقي ملف
   Central Gold (`ADM_CG_SEARCH_LABEL`, `ADM_CG_SEARCH_PLACEHOLDER`,
   `ADM_CG_SEARCH_NO_RESULTS`, `ADM_CG_SEARCH_MIN_CHARS`).

## نقطة مهمة
الـ `do=search` هو أول أكشن GET في هذا الملف (باقي الأكشنز POST-only مع
CSRF token). ده قرار مقصود لتفعيل "بحث أثناء الكتابة" بدون رحلة CSRF لكل
حرف، لكنه يعني إن أي تعديل مستقبلي على هذا الملف لازم يحافظ على إن أكشن
الـ search يفضل **read-only بحت** (مفيش أي INSERT/UPDATE/DELETE تحت
`do=search`) — لو حد أضاف أكشن GET تاني بيغيّر بيانات، لازم يتحط وراء
csrf_verify() أو يتحول لـ POST.

## كيف تجربه
1. بعد التأكد إن `CENTRAL_GOLD_*` متظبطة في `config.php` (الميزة أصلاً
   `fail-soft` — لو مش متظبطة، حقول البحث بتتعطل تلقائيًا `disabled`).
2. ادخل `Admin → Central Gold (Cross-World)`.
3. اكتب أول حرفين من اسم أي لاعب عنده central account في أي حقل بحث —
   المفروض تظهر قائمة منسدلة بالنتائج المطابقة.
4. اضغط على نتيجة → يتملى حقل الإيميل المقابل تلقائيًا → أكمل التحويل/
   اللوكاب عاديًا زي ما كان.
