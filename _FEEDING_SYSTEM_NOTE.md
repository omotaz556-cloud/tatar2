# ملاحظة: نظام العضويات التابعة (Feeding System) — مكتمل

هذا هو مجلد المشروع **الكامل** مع نظام العضويات التابعة (البند رقم 10 في
طلب العميل) مدمجًا بالكامل داخل الملفات. الحالة: **مكتمل من ناحية الكود**
(backend + صفحة أدمن + صفحة لاعب)، لسه محتاج اختبار فعلي على سيرفر PHP
حقيقي قبل النشر للعملاء.

## الملفات الجديدة
- `GameEngine/FeedingSystem.php` — الكلاس الأساسي: إدارة الروابط، الحد
  الأقصى، فحص العلاقة وقت المعركة. بينشئ جداوله بنفسه تلقائيًا
  (`linked_accounts`, `feeding_settings`) — لا حاجة لتشغيل أي SQL يدويًا.
- `GameEngine/Admin/Mods/feedingSystemAdmin.php` — الـ backend الذي يستقبل
  فورم الأدمن (تفعيل/تعطيل، الحد الأقصى، ربط يدوي، حذف رابط).
- `Admin/Templates/feedingSystem.tpl` — صفحة الأدمن (فورم + جدول بكل
  الروابط الحالية).
- `feeding.php` — صفحة اللاعب نفسه لإدارة عضوياته التابعة (self-service،
  ضمن الحد المسموح له من الأدمن).
- `var/db/feeding_system_struct.sql` — توثيق السكيما فقط (مرجعي، غير مطلوب
  تشغيله يدويًا).

## الملفات المعدَّلة (تعديلات صغيرة داخل كل منها فقط)
- `GameEngine/Automation.php` — سطر واحد: تحميل الكلاس الجديد.
- `GameEngine/Automation/AutomationBattleResolution.php` — دالة
  `resolveResourcesAfterBattle()` فقط: عند وجود علاقة "ربط" بين المهاجم
  والمدافع، تُلغى حماية الكرني/المخزن بالكامل لهذه الغارة، فتصبح كل
  الموارد قابلة للنهب (المحدد الوحيد المتبقي هو سعة نقل الجيش نفسه).
- `Admin/admin.php` — إضافة الصفحة الجديدة لقائمة الصفحات المسموحة
  (whitelist) + رابط في القائمة الجانبية.
- `Templates/menu.tpl` — رابط لصفحة اللاعب، يظهر فقط عند تفعيل الميزة من
  الأدمن.
- `GameEngine/Lang/en.php` — كل ثوابت اللغة الجديدة (أدمن + لاعب + رسائل
  الأخطاء)، بنفس أسلوب التسمية المستخدم في باقي المشروع (`tz_def`).

## نقطة معمارية مهمة
هذا نظام **منفصل تمامًا** عن `MultiAccount.php` (كاشف الغش/الحسابات
المتعددة). لم يتم لمس أو تعديل أي سطر في `MultiAccount.php`. كلاهما يعمل
بشكل مستقل، بجداول منفصلة تمامًا.

## كيف تفعّله وتجرّبه
1. انشر الملفات، ادخل على لوحة الأدمن → قسم "Linked Accounts (Feeding)"
2. فعّل الميزة وحدد الحد الأقصى المسموح لكل لاعب (1 أو 2 أو 3... إلخ)
3. إما اربط حسابين يدويًا من نفس الصفحة (بالاسم)، أو خلي اللاعبين يعملوا
   كده بأنفسهم من صفحة `feeding.php` (هتظهر تلقائيًا في قائمتهم بعد
   التفعيل)
4. أي هجوم من الحساب الرئيسي على الحساب المربوط هيتجاوز حماية المخزن
   تلقائيًا من هذه اللحظة

## توصية غير برمجية (موثقة من قبل في `_MASTER_STATUS_AND_LEGAL.md`)
يُنصح بشدة بالإعلان عن هذا النظام رسميًا في قوانين السيرفر المعلنة
للاعبين، لحماية مصداقية السيرفر — الصفحة فيها بالفعل خانة `announced_in_rules`
كتذكير للأدمن، لكنها لا تفرض ذلك برمجيًا.

---
راجع `PROJECT_BRIEF.md` و`_MASTER_STATUS_AND_LEGAL.md` في جذر هذا المجلد
لبقية بنود طلب العميل (الذهب المجاني عبر X-Tatar، تمييز نظام "التتار" عن
Natars، صيغة حساب القوة، إلخ) وحالتها الحالية.
