# تقرير تسليم تقني (Technical Handoff Brief)
## مشروع: لعبة استراتيجية متصفح مبنية على Novaterra — بمواصفات مخصصة من العميل

**الغرض من هذا المستند:** تسليم كامل لأي مساعد AI (Claude Code, GPT, إلخ) أو مطوّر بشري، ليبدأ العمل مباشرة بدون الحاجة لإعادة اكتشاف السياق. هذا المستند نتيجة فحص فعلي للكود المصدري، وليس افتراضات.

---

## 1. السياق العام

- **العميل** طلب مشروع لعبة استراتيجية متصفحية (Browser MMO Strategy) من نوع "Travian-like".
- **الأساس التقني المُختار**: مستودع [Shadowss/Novaterra](https://github.com/Shadowss/Novaterra) — كود مفتوح المصدر (PHP 8.3+, MariaDB/MySQL, رخصة **GPL-3.0**).
- **مراجع طلبها العميل صراحةً كمصدر إلهام للميزات**: موقعي `greek.sa` (سيرفر Travian-like عربي تجاري) و`x-tatar.com` (منصة/سيرفر آخر، على الأرجح فيه نظام "تتار" ونظام تفاعل يمنح ذهب).
- **⚠️ قيد قانوني/تجاري إلزامي**: المشروع الناتج يجب أن يحمل **اسم وهوية بصرية مستقلة تمامًا** (لا اسم Novaterra، لا Greek War، لا X-Tatar، لا أي شعار/تصميم بصري منسوخ منهم). الميكانيكا اللعبية العامة (نظام بطل، تحف، احتلال) هي مفاهيم عامة شائعة في هذا الـ genre وليست حكرًا على أحد، لكن العلامات التجارية والتصميم البصري لكل موقع محمية ويجب تجنبها. كذلك يجب مراجعة نوع ترخيص GPL بدقة (هل AGPL أم GPL عادي) قبل التشغيل التجاري كخدمة، لتحديد الالتزامات المرتبطة بالإفصاح عن الكود المعدّل.

---

## 2. بنية المستودع الأساسي (Novaterra) — ما تم فحصه فعليًا

```
Novaterra/
├── GameEngine/           # منطق اللعبة الأساسي (PHP classes)
│   ├── HeroAdventure.php    # نظام مغامرات البطل (كامل)
│   ├── HeroItems.php        # عناصر/معدات البطل
│   ├── HeroAuction.php      # مزاد عناصر البطل
│   ├── HeroBattleBonus.php  # بونص البطل في القتال
│   ├── GoldShop.php         # أكواد الذهب الترويجية (Promo codes)
│   ├── Artifacts.php        # نظام التحف + توليد قرى الـ Natars
│   ├── Battle.php           # منطق القتال (1973 سطر)
│   ├── Building.php         # منطق البناء والحقول (1888 سطر)
│   ├── MultiAccount.php     # ⚠️ كاشف حسابات متعددة (Anti-Cheat) — انظر القسم 4
│   ├── Village.php          # الإنتاج والموارد لكل قرية
│   ├── Ranking.php          # الترتيب/التصنيفات
│   ├── Automation/
│   │   └── AutomationBattleResolution.php   # فيه handleConquest() — منطق الاحتلال الفعلي
│   └── Database/            # طبقة الاستعلامات
├── Admin/                # لوحة تحكم الأدمن (PHP + Smarty templates)
├── Templates/            # قوالب Smarty للواجهة (dorf1, dorf2, Hero, إلخ)
├── var/db/struct.sql     # ⭐ ملف قاعدة البيانات الكامل — 77 جدول
├── install/              # معالج التثبيت
└── docker-compose.yml    # بيئة تشغيل جاهزة (PHP + MariaDB)
```

**بيئة التشغيل الموصى بها من الـ README الرسمي:**
- PHP 8.3+
- MariaDB (أو MySQL متوافق)
- Docker Compose جاهز: `docker compose up -d` ثم `http://localhost:8080/install`

---

## 3. مطابقة طلبات العميل بالكود الفعلي — جدول تفصيلي

| # | طلب العميل (بالنص) | الحالة الفعلية في الكود | الملفات/الجداول ذات الصلة | التقييم |
|---|---|---|---|---|
| 1 | نظام البطل ومغامراته + إحصائيات (حي/ميت، نقاط، قوة) | **موجود بالكامل** | `HeroAdventure.php`, `HeroItems.php`, `HeroBattleBonus.php`, جدول `hero`, `hero_items`, `hero_adventure` | ✅ جاهز — يحتاج فقط ربط واجهة العرض بالتصميم المطلوب |
| 2 | ذهب مجاني حسب تفاعل العميل على X-TATAR.COM + toggle في الأدمن | **غير موجود** | لا يوجد أي تكامل خارجي حاليًا | ❌ يحتاج بناء كامل (API Webhook + منطق تحويل نقاط→ذهب) |
| 3 | الذهب المشترى غير قابل للتحويل بين حسابات/عوالم | **جزئيًا** — الذهب أصلاً مرتبط بحساب واحد داخل عالم واحد | عمود `gold` في جدول `users` | ✅ السلوك الافتراضي مطابق (لا يوجد تحويل أصلاً) |
| 4 | **الذهب المشترى يفضل مع العميل لو سجّل بنفس الاسم والإيميل في عالم آخر** | **⚠️ غير موجود إطلاقًا — يحتاج تغيير معماري** | — | ❌ **الأهم والأعقد**. كل "عالم" (World/Server) في Novaterra هو **قاعدة بيانات MySQL منفصلة تمامًا** بجدول `users` خاص بها. لا يوجد أي طبقة مركزية تربط الحسابات عبر العوالم. هذا يتطلب تصميم **Central Account/Billing Layer** جديد بالكامل (انظر القسم 5) |
| 5 | نظام التتار (من x-tatar.com) | **موجود جزئيًا كمفهوم عام — Natars** | `Artifacts.php::createNatars()`, جدول `users` (يوجد صف افتراضي باسم `Nature`) | ⚠️ الموجود هو نظام "Natars" (قرى الطبيعة/الحياد) القياسي في أي Travian-clone — **وليس بالضرورة** نفس "نظام التتار" الخاص بـ x-tatar.com. **يحتاج توضيح من العميل**: هل يقصد نفس آلية الـ Natars، أم آلية مختلفة تمامًا موجودة حصريًا في x-tatar.com؟ |
| 6 | نظام التحف (Artifacts) | **موجود بالكامل** | `Artifacts.php` (32K) — `activateArtifacts()`, `addArtifactVillages()`, جداول `artefacts`, `artefacts_chrono` | ✅ جاهز |
| 7 | حساب القوة (Power Calculation) | **موجود بمنطق عام Travian-standard** | `Ranking.php` (procRankArray, procAttRankArray, procDefRankArray, procHeroRankArray) | ✅ الأساس موجود — القوة عادة = دالة في (نقاط الهجوم + الدفاع + عدد الجيش + مستوى الأبنية)، يحتاج تحديد الصيغة الدقيقة المطلوبة من العميل لمطابقة greek.sa تحديدًا (الصيغة الداخلية لـ greek.sa غير معروفة لأن الموقع مغلق المصدر) |
| 8 | آلية الاحتلال (من - إلى) | **موجود ومفصّل** | `AutomationBattleResolution.php::handleConquest()` (يبدأ سطر 560) — يتحقق من: عدد الـ Chiefs الباقين بعد المعركة، مستوى القصر/المقر (Palace/Residence/Command Center)، عدد القرى الحالية مقابل الحد الأقصى المسموح | ✅ جاهز ومطابق للمعيار القياسي في Travian |
| 9 | تحسين UX البناء: الضغط على حقل، الخروج، الرجوع، الضغط تاني بدون "تدبيل" | **الـ Backend جاهز فعليًا — المشكلة على الأرجح Frontend فقط** | `Building.php::canProcess()` (سطر 231) — يتحقق من حالة الحقل مباشرة من قاعدة البيانات (`resarray`, `isLoop()`, `isCurrent()`) في **كل طلب**، أي أن الـ Backend Stateless بالفعل ولا يعتمد على حالة متصفح | ⚠️ يحتاج فحص ملفات JS الخاصة بالبناء (`new.js`, `new2.js`, AJAX templates تحت `Templates/Build/`) لتحديد مصدر مشكلة "التدبيل" إن وُجدت فعليًا |
| 10 | نظام "عضويات تابعة" — هجوم/نهب بلا حد أقصى على الموارد + عدد قابل للتخصيص من الأدمن (1، 2، 3...) | **⚠️ غير موجود — ويوجد نظام معاكس تمامًا في نفس المستودع** | `MultiAccount.php` (610 سطر) | ❌ انظر القسم 4 (تحذير مهم) |

---

## 4. ⚠️ تحذير حرج: `MultiAccount.php`

هذا الملف **ليس** نظام "عضويات تابعة" يسمح بالنهب الحر. هو تحديدًا **نظام اكتشاف غش (Anti-Cheat / Multi-Account Detection)**:

- يحلل: IP مشترك بين حسابات، توقيت تسجيل الدخول المتقارب، بصمة الـ User-Agent، حركة تحويل موارد بين حسابين (Push/Pushing Detection).
- ينتج **تقرير اشتباه (Risk Score 0–100)** لصفحة الأدمن — **لا يحظر أحدًا تلقائيًا**، فقط يبلغ الأدمن/الـ Multihunter للتحقيق اليدوي.
- الجداول المرتبطة: `login_log`, `mad_session`, `resource_transfer_log`.

**الخلاصة:** طلب العميل رقم 10 هو ميزة "Feeding/Pushing" معروفة تجاريًا في بعض سيرفرات Travian الخاصة (يُعلَن عنها رسميًا كقاعدة لعب في بعض السيرفرات). يجب بناؤها **كنظام منفصل تمامًا**، بدون لمس أو تعطيل `MultiAccount.php` الأصلي (الذي يجب أن يبقى فعالاً لصيد الغش غير المُعلَن). يُنصح بشدة أن يُدرَج هذا النظام صراحة في قوانين السيرفر المعلنة للاعبين لحماية مصداقية العميل وسيرفره.

---

## 5. الأنظمة التي تحتاج بناء من الصفر — تفصيل معماري

### 5.1 الذهب المرتبط بالإيميل عبر عوالم متعددة (البند الأعقد)

**المشكلة المعمارية:** كل عالم (سيرفر) = قاعدة بيانات مستقلة (schema منفصل، جدول `users` منفصل بمفتاح `UNIQUE KEY email`). لا يوجد رابط بين قاعدتي بيانات مختلفتين حاليًا.

**الحل المطلوب تصميمه:**
1. إنشاء **قاعدة بيانات مركزية (Central/Master DB)** منفصلة عن قواعد بيانات العوالم، تحتوي:
   - جدول `central_accounts` (بريد إلكتروني كمفتاح فريد، رصيد ذهب مشترى مركزي)
   - جدول `central_gold_transactions` (سجل كل عمليات الشراء/الاستهلاك)
2. عند تسجيل عميل في أي عالم بنفس الإيميل → استعلام على الـ Central DB → مزامنة رصيد الذهب المشترى (وليس المجاني) إلى حساب اللاعب الجديد في العالم الجديد.
3. أي إنفاق للذهب المشترى في أي عالم يجب أن **يُخصَم من الرصيد المركزي** (مزامنة ثنائية الاتجاه أو نظام Ledger/دفتر حسابات).
4. الذهب **المجاني** (من X-TATAR) يبقى محليًا داخل كل عالم فقط، حسب فهمي لطلب العميل — يحتاج تأكيد صريح منه.

**هذا تصميم جديد بالكامل، وليس تعديلاً على `GoldShop.php` الموجود.**

### 5.2 تكامل الذهب المجاني مع X-TATAR.COM

يحتاج:
- توضيح من العميل: هل x-tatar.com ستوفر **API/Webhook** رسمي يرسل بيانات التفاعل؟ أم المطلوب استخراج بيانات (Scraping) — غير موصى به تقنيًا وقانونيًا؟
- جدول جديد لتخزين "نقاط تفاعل X-TATAR" لكل مستخدم وتحويلها إلى ذهب وفق معادلة يحددها الأدمن.
- صفحة في `Admin/admin.php` لتفعيل/تعطيل هذا النظام بالكامل (Toggle) كما طلب العميل حرفيًا.

### 5.3 نظام العضويات التابعة (Feeding System)

يحتاج تصميم من الصفر:
- جدول `linked_accounts` (uid_owner, uid_linked, world_id)
- منطق في محرك القتال (`Battle.php` / `AutomationBattleResolution.php`) يتجاوز حدود النهب القياسية عندما يكون الطرفان مرتبطين في هذا الجدول.
- صفحة أدمن: حقل رقمي `max_linked_accounts_per_player` (قابل للتعديل: 1، 2، 3، إلخ).
- **يجب أن يبقى منفصلاً تمامًا عن `MultiAccount.php`** (القسم 4 أعلاه).

### 5.4 نظام "التتار" (يحتاج توضيح من العميل أولاً)

قبل البدء بالتنفيذ، يجب تحديد: هل هو نفس نظام Natars الموجود أصلاً في `Artifacts.php`، أم آلية مختلفة تمامًا حصرية في x-tatar.com؟ **لا تفترض أي إجابة — اسأل العميل صراحة.**

### 5.5 حساب القوة (Power Formula)

الصيغة الداخلية لـ greek.sa **غير معروفة** (الموقع مغلق المصدر، لا يمكن الوصول لكوده). المطلوب:
- استخدام `Ranking.php` الموجود كأساس (نقاط هجوم/دفاع/عدد جيش/مستوى أبنية).
- تصميم صيغة حساب قوة واضحة موثّقة (Attack Points + Defense Points + Population + Building Levels بأوزان محددة) بدلاً من محاولة "تخمين" صيغة موقع مغلق المصدر.

---

## 6. الترتيب المقترح للتنفيذ (حسب التعقيد والمخاطر)

1. **إعداد بيئة التطوير**: Docker Compose الجاهز من الريبو (`docker compose up -d`)، تشغيل المُثبِّت.
2. **إعادة التسمية والهوية البصرية**: إزالة كل مراجع Novaterra/Travian من الواجهة، القوالب (Smarty)، الشعارات، `gpack/`. هذه خطوة إلزامية أولى قبل أي تخصيص وظيفي.
3. **الأنظمة الجاهزة** (بطل، تحف، احتلال، بناء): مراجعة وتخصيص القيم فقط (لا حاجة لإعادة بناء).
4. **نظام العضويات التابعة (Feeding System)**: تصميم وتنفيذ مستقل.
5. **الذهب المجاني عبر X-TATAR**: بعد الحصول على مواصفات API واضحة من العميل.
6. **الذهب المركزي عبر العوالم**: آخر وأعقد بند — يحتاج تصميم معماري كامل ومراجعة منفصلة قبل البدء (Central DB Layer).
7. **نظام التتار**: بعد توضيح العميل للفرق بينه وبين Natars القياسي.

---

## 7. أسئلة مفتوحة يجب الحصول على إجابتها من العميل قبل بدء التنفيذ

1. هل "نظام التتار" هو نفس Natars القياسي في Travian-clones أم آلية مختلفة في x-tatar.com تحديدًا؟ (اطلب توضيح/سكرين شوتس/رابط توثيق إن وُجد)
2. هل X-TATAR.COM ستوفر API/Webhook رسمي لبيانات التفاعل، أم المطلوب آلية أخرى؟
3. الذهب المجاني (من X-TATAR) — هل يبقى محليًا لكل عالم أم يُزامَن مركزيًا مثل الذهب المشترى؟
4. نظام العضويات التابعة (Feeding) — هل سيُعلَن رسميًا في قوانين السيرفر للاعبين، أم يبقى غير معلن؟ (توصية: يجب الإعلان عنه لحماية مصداقية السيرفر)
5. صيغة حساب القوة المطلوبة تحديدًا — هل هناك وثيقة أو مرجع رقمي دقيق (وليس فقط "زي greek.sa") يمكن العمل عليه؟
6. نوع ترخيص Novaterra بالتحديد (GPL-3.0 عادي أم AGPL) — يجب مراجعته قانونيًا قبل الإطلاق التجاري كخدمة.

---

## 8. ملاحظة ختامية للـ AI/المطوّر القادم

- لا تحاول "تخمين" منطق greek.sa أو x-tatar.com الداخلي — هذان موقعان تجاريان مغلقا المصدر، وأي محاولة استنتاج شكل الكود الداخلي لهما من الوصف الخارجي فقط ستكون غير دقيقة. اعمل على أساس المواصفات الوظيفية (Functional Requirements) المذكورة أعلاه، وصمم حلاً هندسيًا مستقلاً يحقق نفس الوظيفة.
- كل ما ورد في القسم 3 مبني على فحص فعلي لملفات الكود المصدري لـ Novaterra (تم استنساخه وفحصه سطرًا بسطر في التواريخ ذات الصلة)، وليس افتراضات.
- المشروع النهائي يجب أن يكون **منتجًا بهوية مستقلة بصريًا وتجاريًا**، حتى لو ظلت الميكانيكا الداخلية للعبة مبنية على Novaterra.
