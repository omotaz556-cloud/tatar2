# ملاحظة: نظام "التتار" = Natars — تم توضيحه من العميل، والتسمية اتظبطت

العميل أكّد صراحة: **"تتار = NATARS"**. يعني نظام "التتار" اللي طلبه مش
آلية لعب مختلفة محتاجة بناء من الصفر — هو نفس نظام قرى الطبيعة/الحياد
(Natars) القياسي الموجود بالفعل في `GameEngine/Artifacts.php`، بس
بمسمى مختلف للاعب.

## اللي اتعمل
غيّرنا **النص المعروض فقط** من "Natars" لـ "Tatars" (وكل الصيغ اللغوية
المشتقة منها) في ملفات اللغة الستة كلهم:
`GameEngine/Lang/{en,es,fr,it,ro,zh}.php`

### التفاصيل التقنية
- الاستبدال اتعمل بدقة على **قيمة كل `tz_def('KEY', 'value')`** فقط،
  مش على اسم الـ KEY نفسه. اتأكدنا برمجيًا (مقارنة قائمة كل الـ keys
  قبل/بعد لكل لغة) إن عدد وترتيب كل الـ keys **مطابق 100%** بعد التعديل.
- الصيغ اللي اتغيّرت: `Natars/NATARS/natars`, `Natar/NATAR/natar`,
  `Natarian/NATARIAN/natarian`, `Natares` (إسباني), `Natari/Natarilor`
  (روماني), `纳塔` (صيني) → المقابل بـ "Tatar" بنفس الحالة (uppercase/
  lowercase/title case) ونفس القواعد اللغوية لكل لغة.
- تأكدنا إن صفر حالات متبقية داخل أي `value` معروضة (فحص شامل بعد
  التعديل، بمعزل عن أي تعليقات كود `//` اللي فيها الكلمة ولسه موجودة
  عادي لأنها مش معروضة للاعب أصلاً).

### اللي اتعمّد إنه يفضل زي ما هو (مش له تأثير على اللاعب)
- أسماء الـ PHP constants الداخلية: `Artifacts::NATARS_UID`,
  `Artifacts::NATARS_TRIBE`, `Artifacts::NATARS_EMAIL`، وأسماء مفاتيح
  اللغة نفسها زي `NATARS_ACC`, `NATAR_COUNTERFORCE`, `ADM_5_NATARS`.
- أسماء الدوال: `Artifacts::createNatars()`,
  `Artifacts::returnArtifactToNatars()`.
- أسماء الملفات: `Templates/natars.tpl`, `Admin/Templates/natars.tpl`,
  `GameEngine/Automation/AutomationNatarsWW.php`, إلخ.
- أعمدة/جداول قاعدة البيانات (لو موجودة بنفس الاسم).

تغيير الأسماء الداخلية دي كان هيبقى تعديل ضخم عالي الخطورة (73+ ملف)
من غير أي فايدة للاعب، فاتجنّب عمدًا.

## لغة عربية
لسه مفيش ملف لغة عربي (`ar.php`) في المشروع — كل اللغات الموجودة حاليًا
(en/es/fr/it/ro/zh) اتظبطت لعرض "Tatars" بلغتها. لو العميل عايز واجهة
عربية كاملة فيها "التتار" بالعربي، ده بند منفصل يحتاج ترجمة كاملة
لآلاف الأسطر (~3900 مفتاح لغة) — لازم يتطلب صراحة كبند مستقل.
