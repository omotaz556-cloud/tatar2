# البند 14 — Visual density: خطة فحص عملية على المتصفح

هذا البند، زي ما هو مكتوب في الفحص الأصلي، **مش قابل للتحقق من الكود
لوحده** — الكثافة البصرية (تباعد، حجم خط، ازدحام العناصر) حكم بصري
محتاج فتح الصفحات فعليًا على متصفح حقيقي وشاشات مختلفة. اللي أقدر
أساعد بيه هنا هو تجهيز خطة فحص دقيقة ومربوطة بأماكن محددة في الكود،
بدل نصيحة عامة.

## أولوية الفحص (الأماكن الأكثر عرضة لمشكلة كثافة)

1. **شريط الموارد + طابور البناء** (`Templates/res.tpl`, `Templates/Building.tpl`)
   يتحدّث بالـ AJAX على كل عملية بناء (`build.php`) — أكتر عنصر بيتكرر ظهوره
   في الشاشة، أي ازدحام هنا بيتحس بسرعة.

2. **صفحة الأدمن — الجداول الطويلة** (`Admin/Templates/*.tpl`,
   خصوصًا `config.tpl` و`featureFlags.tpl` الجديد و`admin_log.tpl`)
   عدد أعمدة كبير + خط صغير (font-size:9-11px موجود فعلاً في CSS
   الأدمن) = أول مكان هيبان فيه الازدحام على شاشة أصغر.

3. **جدول التقارير / الرسائل** (`berichte.php`, `nachrichten.php`)
   جداول بيانات كثيفة أصلاً في التصميم الأصلي (Travian-style).

4. **الصفحات اللي اتلمست في البند 15 (Responsive)** — بعد إضافة
   `transform: scale()` في `gpack/_responsive/responsive.css`، لازم
   تتأكد إن التصغير (0.82 على تابلت، 0.46 على موبايل) مايخليش الخط
   يبقى صغير جدًا أو العناصر تتلزق في بعض. النسب دي **تخمين مبدئي**
   محتاج ضبط فعلي بعد الفتح على أجهزة حقيقية.

## خطوات الفحص المقترحة

- [ ] افتح `dorf1.php` و`dorf2.php` على 1920px, 1366px, 768px (تابلت), 390px (موبايل)
- [ ] افتح شاشة البناء (`build.php?id=X`) وجرب زرار "إنهاء بالذهب" — تأكد الرسالة/الحالة وضحة لما الفلاج `gold_finish_building` يبقى OFF من `?p=featureFlags`
- [ ] افتح `Admin/admin.php?p=featureFlags` نفسها وقيّم وضوح الجدول على شاشة صغيرة
- [ ] افتح `berichte.php` بتقرير طويل وقارن قبل/بعد تفعيل `.mobile-scroll-x` (المُضاف في `responsive.css`)
- [ ] جرب تكبير/تصغير نص المتصفح (Ctrl + / Ctrl -) على صفحتين على الأقل للتأكد من عدم انكسار التصميم عند تكبير الخط لأسباب accessibility

## لو ظهر ازدحام فعلي بعد الفحص

وثّق كل مشكلة بصورة screenshot + اسم الملف/القالب المسؤول، وابعتها في
رسالة جديدة — وقتها أقدر أعدّل الـ CSS/الـ padding في الملف الصحيح
مباشرة بدل تخمين قيم من غير معاينة، وده أضمن من محاولة "تحسين" الكثافة
بشكل عشوائي في كود بحجم المشروع ده.
